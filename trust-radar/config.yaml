# Non-secret configuration (secrets go in .env)
account_name: "Acme Corp"
csm_name: "Taylor Brooks"
provider:
  llm: manual       # anthropic | openai | manual
  crm: manual       # manual | salesforce | hubspot
  transcript: manual  # manual | gong | fireflies
